##We have to delete a container that was created for testing purposes:

See all containers(running, stopeed, exited):
docker ps -a

Stop that container:
docker stop cont_name

Remove that container:
docker rm cont_name
