##We have to delete a container that was created for testing purposes:<br>

See all containers(running, stopeed, exited):<br>
docker ps -a

Stop that container:<br>
docker stop cont_name

Remove that container:<br>
docker rm cont_name
